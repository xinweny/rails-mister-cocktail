<%= simple_form_for @cocktail do |f| %>
  <%= f.input :name %>
  <% if @cocktail.id.nil? %>
    <%= f.submit 'Create!', class: 'button cocktail-button rounded' %>
  <% else %>
    <%= f.submit 'Change', class: 'button cocktail-button rounded' %>
  <% end %>
<% end %>
